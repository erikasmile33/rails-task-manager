<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<input type="checkbox" name="completed"
  <%= "checked" if @task.completed %>
  onclick="return false;" />

<%= link_to "Delete task", task_path(@task), class:"task-link",
data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
